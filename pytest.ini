[pytest]
testpaths = tests
asyncio_mode = auto

# Anciens tests squelettes qui supposent un cwd dans le service correspondant
# (ex: tests/unit/test_streaming.py importe `app.streaming` directement).
# Ils seront migrés vers le nouveau pattern dans tests/conftest.py.
norecursedirs = tests/unit tests/e2e
